{
  "name": "editor",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "eslint": "eslint src --ext .ts,.vue --fix",
    "stylelint": "stylelint --fix src/**/**/*.{css,scss}",
    "lint": "pnpm eslint && pnpm stylelint"
  },
  "license": "MIT",
  "dependencies": {
    "@lc/shared": "workspace:^0.0.1",
    "pinia": "^2.0.14",
    "vue-router": "^4.0.15"
  }
}
